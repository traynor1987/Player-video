# Security

Do not commit or attach IPTV credentials, private playlist URLs, XMLTV URLs, keystores, passwords, or Base64 keystores. Source secrets are encrypted on device with an Android Keystore AES-GCM key. Network errors shown to users are generic and must never include credential-bearing URLs.

Report security concerns privately to the repository owner rather than opening an issue containing secrets.
